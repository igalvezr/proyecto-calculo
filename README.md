# Análisis de funciones
Este repositorio contiene el proyecto de la materia "Cálculo y Geometría Analítica".

Corresponde al trabajo realizado por los integrantes indicados en el reporte, pertenecientes al grupo no. 34.

## Instalación
### Dependencias
Éste proyecto fué desarrollado con un ambiente virtual de _conda_, utilizando la versión **3.13.7** de python. Las dependencias son las siguientes:

- python 3.13.7
- matplotlib
- numpy

La instalación del intérprete de python depende del sistema. Para **windows**, se puede obtener desde la página oficial [python.org](python.org) o desde la tienda de microsoft; para sistemas **Linux**, puede encontrarse utilizando el gestor de paquetes de la plataforma. Una vez instalado, se puede verificar la instalación utilizando el comando:
> `$ python --version`

Que debería imprimir la versión de python instalada.

La instalación de las dependencias se realiza con el comando:

> `$ pip install matplotlib numpy`

## Instalación - proyecto
Para ejecutar el proyecto, y una vez teniendo el intérprete de python disponible en el sistema, así como las dependencias del proyecto, se debe clonar el presente repositorio usando git:

> `$ git clone https://github.com/igalvezr/proyecto-calculo.git`

Una vez clonado el repositorio, se debe navegar a la carpeta raíz, desde donde se ejecuta el script principal con el siguiente comando:

> `$ python main.py`